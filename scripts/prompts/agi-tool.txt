A left-to-right flow on an operations screen. On the left, a terminal window with a simple top bar and a blinking cursor, labeled "I decide" above it. A green arrow goes right into a large violet machine block in the middle, like a powerful engine or a set of interlocking gears inside a rounded housing, labeled "Tool". A second green arrow goes right from the tool into a stack of three document cards labeled "Result". From the result, a thin amber arrow curves back underneath to the terminal on the left, labeled "Review". The machine block is large and impressive but has no face and no eyes; it is clearly a machine that only moves when the terminal sends it work.
Only these labels: "I decide", "Tool", "Result", "Review".
