A wide horizontal pipeline diagram, read left to right, made of rounded station boxes joined by phosphor green (#52F184) arrows.

1. Far left, amber (#FFBE3D): a stack of cards with small tag pills, labelled "Open PRs and issues".
2. Then the flow splits into two parallel violet stations stacked vertically: a lens over a pull-request icon labelled "pr-audit" (monospace), and a lens over a bug icon labelled "iss-audit" (monospace).
3. Both merge into one brighter violet station with a wrench and a test-tube icon, labelled "github-resolution" (monospace). A small green status dot under the arrow before it, labelled "Approved".
4. Then a violet station with a shield and a checklist, labelled "pr-post-audit" (monospace).
5. Then a rose (#F45FB0) station with a tag and a package box, labelled "release" (monospace).
6. Far right, a teal (#2FD8D0) station with a gear and a small upward curved arrow, labelled "Kaizen". From it, one long thin teal arrow curves back along the top of the whole diagram to the far left station, closing the loop.

Beneath the whole pipeline, a thin teal bar spanning the full width, labelled "ai-memory".

Only these texts appear in the image, each exactly once: "Open PRs and issues", "pr-audit", "iss-audit", "Approved", "github-resolution", "pr-post-audit", "release", "Kaizen", "ai-memory". No other words or letters.
