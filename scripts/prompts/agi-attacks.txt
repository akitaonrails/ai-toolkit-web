A left-to-right scene on an operations screen. On the left, a large rectangular enclosure drawn with thick glowing orange walls, like a secured cage or sandbox, labeled "Sandbox" above it. Inside the enclosure sits a violet machine block (a faceless engine with small gears, clearly a tool, not a creature). The right wall of the enclosure has a wide doorway whose door hangs wide open on its hinge, and the gap glows amber; under the gap the label "Left open". A thin violet line runs from the machine through the open doorway to a row of three blue server racks on the right side of the frame, labeled "Real servers". On the floor just outside the doorway lies a small glowing amber key on a ring, labeled "Credentials". Above the doorway, a small terminal window with a blinking cursor and a thin line down to the door's handle, labeled "Human decision". The picture says: the tool did not break out; a person left the door open and the keys outside.
Only these labels: "Sandbox", "Left open", "Real servers", "Credentials", "Human decision".
